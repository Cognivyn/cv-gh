# Changelog

## 0.1.0

- Added the initial `cv-gh` CLI with `init`, `status`, `doctor`, `pr`, and `release` commands.
- Added Rust tests, CI checks, and release artifact workflows.
- Added npm wrapper installation from GitHub release artifacts.
